# 빌드 컨텍스트에서 제외 — 전송량을 줄이고, 시크릿이 이미지에 섞이는 것을 막는다.
node_modules
dist
coverage
.git
.gitignore

# 시크릿: .env* 는 절대 이미지에 들어가면 안 된다.
# 운영 값은 컨테이너 환경변수로 주입한다(~/docs/fleet/konnect-deploy.md).
.env
.env.*
!.env.example

*.log
.DS_Store
test
